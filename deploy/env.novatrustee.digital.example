# Copy to repo root as .env on the VPS (never commit .env):
#   cp deploy/env.novatrustee.digital.example .env
# Edit ONEX_API_KEY before starting.

ONEX_API_KEY=CHANGE_ME_LONG_RANDOM_SECRET
ONEX_CORS_ORIGINS=https://novatrustee.digital,https://git.anakatech.llc
